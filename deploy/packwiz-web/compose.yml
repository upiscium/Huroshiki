services:
  packwiz-web:
    image: nginx:alpine
    container_name: packwiz-web
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - ./packs:/usr/share/nginx/html:ro
      - ./default.conf:/etc/nginx/conf.d/default.conf:ro
